.card
  %header.card-header
    %p.card-header-title= product.name
  .card-content
    .content
      = product.description
    - unless product.information.none?
      .columns
      - product.information.each do |information|
        .column
          %article.message
            .message-header
              %p= information.name
              %small
                = link_to 'Modifier cette information', edit_product_information_path(product, information)
            .message-body
              %p= simple_format information.contents
              %br
  %footer.card-footer
    = link_to 'Modifier', edit_product_path(product), class: 'card-footer-item'
    = link_to 'Rajouter une information', new_product_information_path(product), class: 'card-footer-item'
