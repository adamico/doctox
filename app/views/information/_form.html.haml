= form_for [product, information], role: 'form' do |f|
  - if information.errors.any?
    #error_explanation
      %h3
        = pluralize(information.errors.count, 'erreur', plural: 'erreurs')
        empêche#{information.errors.many? ? 'nt' : ''}
        la sauvegarde :
        %ul
          - information.errors.full_messages.each do |msg|
            %li= msg

  = f.label :name, class: 'label'
  %p.control= f.text_field :name, placeholder: t('attributes.name'), class: 'input'

  = f.label :contents, class: 'label'
  %p.control= f.text_area :contents, placeholder: t('attributes.contents'), class: 'textarea'

  %p.control= f.submit 'Enregistrer', class: 'button is-primary'
